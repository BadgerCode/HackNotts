<phone:PhoneApplicationPage
    x:Class="Timetabling_App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="WEEK 11" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0" Height="31"/>
        </StackPanel>


        <Grid Grid.Row="1" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height ="Auto"/>
                <RowDefinition Height ="Auto"/>
                <RowDefinition Height ="Auto"/>
                <RowDefinition Height ="Auto"/>
                <RowDefinition Height ="Auto"/>
            </Grid.RowDefinitions>

            <!--ContentPanel - place additional content here-->
            <Border BorderBrush="White" BorderThickness="1">

                <Grid x:Name="Day1"  Grid.Column="0" Grid.Row="0" HorizontalAlignment="Stretch" Height="116" VerticalAlignment="Bottom" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="83*"/>
                        <ColumnDefinition Width="21*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text ="Monday" Height="116" HorizontalAlignment="Stretch" Grid.ColumnSpan="2"/>
                    <TextBlock Text ="Monday" Height="116" HorizontalAlignment="Stretch" Grid.ColumnSpan="2"/>
                    <TextBlock Text ="Monday" Height="116" HorizontalAlignment="Stretch" Margin="0,29,0,-29" Grid.ColumnSpan="2"/>
                    <TextBlock Text ="Monday" Height="116" HorizontalAlignment="Stretch" Grid.ColumnSpan="2"/>
                    <TextBlock Text ="Monday" Height="116" HorizontalAlignment="Stretch" Grid.ColumnSpan="2"/>
                    <TextBlock HorizontalAlignment="Stretch" Margin="75,0,-376,84" Grid.Column="1">
                	    <Run Text="17:00 - 18:00 : G52AFP - "/>
                	    <Run Text="BSouth A32"/>
                    </TextBlock>

                </Grid>
            </Border>

            <Grid x:Name="Day2" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch">
                <Border BorderBrush="White" BorderThickness="1">
                    <TextBlock Text ="Tuesday"  Height="116"/>
                </Border>
            </Grid>
            <Grid x:Name="Day3" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Stretch">
                <Border BorderBrush="White" BorderThickness="1">
                    <TextBlock Text ="Wednesday"  Height="116"/>
                </Border>
            </Grid>
            <Grid x:Name="Day4" Grid.Column="0" Grid.Row="3" HorizontalAlignment="Stretch">
                <Border BorderBrush="White" BorderThickness="1">
                    <TextBlock Text ="Thursday" Height="116"/>
                </Border>
            </Grid>
            <Grid x:Name="Day5" Grid.Column="0" Grid.Row="4" HorizontalAlignment="Stretch" >
                <Border BorderBrush="White" BorderThickness="1">
                    <TextBlock Text ="Friday"  Height="116"/>
                </Border>
            </Grid>

        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->

    </Grid>



    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>